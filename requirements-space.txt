# CalRetail — Hugging Face Space requirements.
#
# A trimmed copy of requirements.txt for container deploy. Confirmed unused at
# runtime (grepped: no imports outside the legacy frontend/ Streamlit app,
# scripts, or notebook-authoring tools) and dropped: streamlit, nltk, faker,
# jupyter/ipykernel/nbformat, pytest, openpyxl. Everything actually imported by
# backend/ or frontend_dash/ — including matplotlib (05_demand_forecasting.ipynb)
# and scipy (adaptive_thresholds.py) — stays pinned exactly as in requirements.txt.

langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
python-dotenv>=1.0.0

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
scipy>=1.11.0
matplotlib>=3.8.0

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
httpx>=0.26.0

dash>=3.3.0
plotly>=5.18.0

loguru>=0.7.0
tqdm>=4.66.0
requests>=2.31.0
